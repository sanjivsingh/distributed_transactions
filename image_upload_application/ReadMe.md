# Implement Image Upload Service:


-   HTTPS for secure communication
-   Use JWT for authentication
-   create user profile
-   integrate gravator for user image
-   Generate S3 pre-signed URL on the fly going through the above documentation
-   Sign-up on Cloudflare, Akamai, or BunnyCDN and explore their features like Origin Configuration, TTL, image Optimization
-   Configure a website as origin for CDN and see how CDN improves the latency
-   Pass query parameters to resize and transform the image served via CDN
-   Try invlidating a CDN path and measure the time it takes to reflect
-   invalidate CDN Cache using surrogate keys
-   Implement a Toy CDN
-   Implement pre-signed URL based upload on S3
-   Mimick CDN Failover
-   Configure multiple properties on same CDN under different CDN host-names
-   Configure same website as origin for it.

